= render :partial=>"/scraps/search_form"
%h1 Scraps from observed
- @scraps.each do |scrap|
  = render :partial => '/scraps/single_scrap', :locals=>{:scrap=>scrap, :show_all=>false}
= paginate @scraps
