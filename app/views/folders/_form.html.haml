= simple_form_for([@organization, @folder], :html=>{:class=>"form-horizontal"}) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name

  .form-actions
    = f.button :submit, class: 'btn btn-success'
